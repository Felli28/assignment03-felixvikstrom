name: Pipeline
on:
  push:
    branches:
      - main
      - gh-actions

jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
         
      - name: Install dependencies
        run: npm install

      - name: Install Playwright
        run
